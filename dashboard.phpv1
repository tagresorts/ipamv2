<?php
session_start();
include 'config.php';

// Redirect if not logged in
if (!isset($_SESSION['user_id'])) {
  header("Location: login.php");
  exit;
}

// Fetch IPs
$stmt = $pdo->query("SELECT * FROM ips");
$ips = $stmt->fetchAll();
?>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dashboard - IP Management System</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css?family=Open+Sans:400,600&display=swap" rel="stylesheet">
  <!-- Custom Stylesheet -->
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="navbar">
    <div class="logo">
      <h2>IP Management System</h2>
    </div>
    <div class="user-info">
      <p>Welcome, <strong><?= htmlspecialchars($_SESSION['username'] ?? 'User') ?></strong> (Role: <?= htmlspecialchars($_SESSION['role']) ?>)</p>
    </div>
    <div class="nav-links">
      <a href="dashboard.php" class="nav-btn">🏠 Home</a>
      <a href="add_ip.php" class="nav-btn">➕ Add IP</a>
      <a href="subnets.php" class="nav-btn">🌐 Subnets</a>
      <?php if ($_SESSION['role'] === 'admin'): ?>
        <a href="admin_users.php" class="nav-btn">👥 Manage Users</a>
      <?php endif; ?>
      <a href="logout.php" class="logout-btn">🚪 Logout</a>
    </div>
  </div>


  <div class="container">
	<h3 class="ip-list-title">📋 IP Address List</h3>
    <?php if (count($ips) > 0): ?>
      <table>
        <tr>
          <th>IP Address</th>
          <th>Status</th>
          <th>Assigned To</th>
          <th>Owner</th>
          <th>Description</th>
        </tr>
        <?php foreach ($ips as $ip): ?>
          <tr>
            <td><?= htmlspecialchars($ip['ip_address']) ?></td>
            <td><?= htmlspecialchars($ip['status']) ?></td>
            <td><?= htmlspecialchars($ip['assigned_to']) ?></td>
            <td><?= htmlspecialchars($ip['owner']) ?></td>
            <td><?= htmlspecialchars($ip['description']) ?></td>
          </tr>
        <?php endforeach; ?>
      </table>
    <?php else: ?>
      <p>No IP addresses found. <a href="add_ip.php">Add your first IP</a></p>
    <?php endif; ?>
  </div>
</body>
</html>
